{
  "version": 2,
  "buildCommand": "cd client && npm install && npm run build",
  "outputDirectory": "client/build",
  "github": {
    "silent": true
  },
  "rewrites": [
    { "source": "/api/analyze-track", "destination": "/api/analyze-track" }
  ]
}
